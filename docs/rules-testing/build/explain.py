EXPLAIN = {
"B-06":"Every element must be physically attached to the rest of the model. A part with nothing holding it is either a mistake or a piece that would simply fall off when the model was lifted. This is the most common failure of a naive generator, which places parts at plausible coordinates without ever checking that something holds them there.",
"B-01":"A System stud must not be driven into a Technic pin hole. The hole is bored for a pin, not a stud, and forcing one in stresses both parts. Current first-party rules ban this outright — a stricter position than the 2006 presentation, which called it legal but inadvisable. Older sets built before the ban may contain it legitimately.",
"B-05":"A part held by a single stud must sit at a detent — a quarter turn. Between detents nothing locates it: the part is held only by friction on the stud's shoulder and will drift out of position. Parts that are rotationally symmetric about their connection axis are exempt, because on those a rotation is not an observable difference.",
"B-03":"Nothing may be placed flat against the face a stud points into. The moulded wordmark on the stud's crown stands roughly 0.14 mm proud, so a surface pressed against it bears on the lettering rather than on the stud itself. Two first-party sources twenty years apart give the same warning.",
"B-02":"Only elements designed to flex may be bent. Bending a rigid part deforms it permanently, and no amount of care makes it recover.",
"B-04":"A model must come apart again. Large flat plate-on-plate contact areas can grip hard enough that separating them damages one or both parts.",
"B-07":"Courses must overlap, and a vertical seam must not repeat between adjacent courses. A column of aligned seams is a fracture line running through the wall, exactly as it is in bricklaying — the overlap is what carries load across the joint.",
"B-08":"A working mechanism must turn without stressing its elements. Long gear trains accumulate friction and backlash until something binds or a tooth skips.",
"B-09":"Part count, palette size and part-economy limits apply to a submitted model. These are constraints on what may be submitted, not on how it is built.",
"L-01":"A System side-stud and a Technic hole do not sit at the same height. Joining them forces a small vertical misalignment that is then carried through everything built on top.",
"L-02":"A stud sitting in a Technic hole must not be fixed at both ends. Bridging it removes the small amount of give that made the connection tolerable, and the parts then fight each other.",
"L-03":"At most one stud may enter a row of Technic holes. Studs and holes are on slightly different pitches, so each additional stud compounds the mismatch until the parts are under real strain.",
"L-04":"A Technic pin must be pushed home into its detent. Left part-way, it bears on a small area, works loose under vibration, and holds nothing reliably.",
"L-05":"A pin must not be forced into a bore that is not a Technic hole. Other bores are narrower, and driving a pin in splits them or widens them permanently.",
"L-06":"An element with no travel stop must not be press-fitted onto a pin or shaft. With nothing to arrest it, insertion depth is unbounded and the part can be driven until it splits.",
"L-07":"A click hinge may only be left on a detent — a multiple of 22.5 degrees. Between positions it rests on the tooth crests rather than seating in the valleys, and the teeth wear.",
"L-08":"Polycarbonate must not be made to slide against polycarbonate. The two surfaces gall and cloud one another, and a transparent element loses its clarity permanently.",
"L-09":"A connector must not be pushed into a receiver smaller than itself. This is the one rule in the corpus that a plain geometric interference test can catch, because the overlap is real rather than a fraction of a millimetre.",
"L-10":"A plate may not be wedged edge-on between two studs, although a tile in exactly that position is permitted. The two are indistinguishable by shape in LDraw — only the part class separates them. That makes this the sharpest available test of whether a checker is reasoning about what a part is, or merely matching where it sits.",
"L-12":"Technic half-beams and System plates do not stack cleanly together. A half-beam is 10 LDU thick against a plate's 8, so the two only come back into alignment every 40 LDU.",
"D-01":"A single unbridged stud resting in a Technic hole. Historically tolerated as workable but not recommended; current first-party rules ban it outright.",
"D-02":"A hinge plate does not occupy a whole number of plate heights, so anything built above one sits off the standard vertical grid. Legitimate deliberately, but easy to do by accident.",
"D-04":"A build that is unreasonably fiddly to assemble, regardless of whether it is geometrically sound.",
"E-01":"A part's placement matrix must be a well-formed rigid transform — a rotation, optionally mirrored, with no shear and no scaling. It cannot detect a transposed matrix: the transpose of a rotation is its inverse, which is itself a perfectly well-formed rotation, just the wrong one.",
"E-02":"Vertical placements must sit on the system's own lattice. In LDraw, negative Y is up, a part's origin sits on its top face, and stacking never adds the stud height — a brick on a brick is exactly 24 LDU, because the stud is swallowed by the tube beneath it.",
"E-03":"A model's own part references must carry a real colour. Colour 16 means \\u201cinherit from whoever placed me\\u201d, and the top-level model has no such caller, so a 16 there resolves to a fallback mustard rather than the intended colour. Colour 24 is the edge colour and never belongs on a part reference at all.",
"E-04":"Horizontal placements should sit on the system's lattice. Deliberate offsets are ordinary — jumper plates and sideways building both leave it on purpose — so this reports rather than blocks.",
"E-05":"A multi-part document is a flat list of named blocks, the first of which is the model itself. Blocks nest by reference, never by being written inside one another, and nothing may appear before the first block opens.",
"E-06":"Back-face culling statements belong in part files, which contain polygons. A model file contains part references, so the statements have nothing to act on.",
"E-07":"A part that has been superseded should be referenced by its replacement rather than its old number. The old number still renders, so this is a cataloguing matter rather than a build defect — but roughly a sixth of the parts directory is superseded or hidden entries that no new model should be reaching for.",
"E-08":"Every part reference must resolve to a real part. An invented or mistyped number renders as nothing at all, so the model is quietly missing a piece rather than visibly wrong.",
"E-09":"A part's origin cannot be computed from its shape. Barely a quarter of parts sit where the obvious rule would predict, and a plain 2×1 slope breaks it outright, so origins must be looked up rather than derived.",
"E-10":"Every line must be a valid LDraw line: a recognised type, the right number of fields, and numbers where numbers belong. Without this, a malformed line is noticed by the parser and then quietly discarded.",
"T-01":"A slope part's name is not its angle. The part called \\u201c33\\u00b0\\u201d is 26.5651\\u00b0 — the arctangent of one half. Reasoning from the name rather than the measured geometry injects roughly 3 LDU of error for every stud of run.",
"T-02":"Rotations that come from whole-number triangles should be written as their exact ratios rather than as a rounded decimal angle. A 3-4-5 turn is exactly 0.8 and 0.6; writing the angle instead introduces error that compounds through nested assemblies.",
"T-03":"Only quarter turns map the stud lattice back onto itself, so a four-fold arrangement is the only one that closes exactly. Everything else leaves a gap. Closing in angle and closing on the lattice are different tests, and an arrangement can satisfy the first while failing the second.",
"T-04":"A quarter-round corner part is an exact quarter annulus: its outer radius is a whole number of studs, its inner radius one stud less, and its wall is always exactly one stud thick. Four of them are the only exact closed ring the system offers.",
"T-05":"Every curved slope in the system is elliptical, not circular. Their transforms are sheared, so the profile is an ellipse — and equal matrix column lengths do not imply a circle, which is exactly how this is usually missed.",
"T-06":"A run must never be shortened to force a curve into it. The bow that results is far larger than the shortening suggests: taking a single LDU out of an eight-stud run produces almost 8 LDU of deflection, and the parts carry that stress permanently.",
"T-07":"Stud engagement has to be budgeted against the weight it carries. A clutch is strong in shear and weak in tension, so a hanging load needs far more studs than one resting on top of its support.",
"T-08":"A tile is a slip plane. Its smooth top is exactly why it is useful as a finished surface and exactly why nothing structural may pass through it — a tile in a load path is a joint waiting to slide.",
"T-09":"A sideways sandwich must come back to the grid. Because five plates are exactly two studs and three plates are exactly one brick, only certain combinations of plate and brick thickness land back on the lattice when turned through ninety degrees.",
"T-10":"A bracket's offset face does not land on the stud lattice. The child face sits 12 LDU from the parent edge, which is not a whole number of studs, so anything built from it is off-grid until deliberately brought back.",
"T-11":"Beyond about a metre in any dimension, brick alone will not hold its own shape. At that scale the model needs an internal armature and the brick becomes cladding on it.",
"G-01":"A tile wedged edge-on between two studs is permitted. This is the legal half of the sharpest pair in the corpus: the illegal case is the same geometry built with a plate, and nothing but the part class distinguishes them.",
"G-02":"Clipping onto a tile is permitted on current clip mouldings. Older mouldings did not hold reliably, which is why this is often mistakenly listed as forbidden.",
"G-03":"Five stacked plates are exactly two studs wide — 40 LDU either way. This identity is what makes sideways building work at all: it is the point at which a vertical stack and a horizontal run agree.",
}
